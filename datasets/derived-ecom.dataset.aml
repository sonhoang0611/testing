import '../models/base-model/users.model.aml' { users }
import '../models/base-model/orders.model.aml' { orders as o } 

Dataset raw_ecommerce {
  label: 'Raw Ecommerce'
  description: "This Dataset is about Ecommerce data nha"
	owner: 'khai@holistics.io'
  
  data_source_name: 'demodb'

  models: [
    users,
    o
  ]

  relationships: [relationship(o.user_id > users.id, true)]
}

raw_ecommerce